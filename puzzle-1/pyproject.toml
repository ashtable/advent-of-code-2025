[project]
name = "puzzle-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.ruff]
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "B", "ANN", "TCH"]
per-file-ignores = { "tests/*" = ["ANN"] }

[tool.pyright]
typeCheckingMode = "strict"
pythonVersion = "3.13"

[dependency-groups]
dev = [
    "pyright>=1.1.407",
]
